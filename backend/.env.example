# Copy this file to .env and fill in real values for local testing.
# Without these set, the server still runs fine — the notifications route
# just logs emails instead of sending them.

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER="txylor2016@gmail.com"
SMTP_PASS="hudw ybrr nuyw gxhp"
SMTP_FROM="The LineUp <txylor2016@gmail.com>"

# Optional — defaults to 4000 if not set
PORT=4000
